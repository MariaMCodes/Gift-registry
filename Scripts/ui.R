
library(shiny)

ui <- fluidPage(
  fluidRow(
    h4("Apple's Birthday Gift Registry", align = "center", style = "color:darkblue"),
    img(src = "Header.png", align = "center", height = 200, width = 400)
  )
)
